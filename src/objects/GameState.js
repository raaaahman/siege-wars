class GameState {
    constructor(previousState, scene) {
        this.activePlayer = previousState.activePlayer
        this.scene = scene
    }

    pointerMove() {

    }

    pointerDown() {
        
    }
}

export default GameState